<%= button_tag request.processed ? t(:unapprove) : t(:approve),
  class: "btn btn-full-size #{"btn-warning" unless request.processed}",
  onclick: "approveRequest(#{request.id},#{!request.processed})" %>
