<%= button_tag request.book.bought ? t(".not_bought") : t(".bought"),
  class: "btn btn-full-size #{"btn-info" unless request.book.bought}",
  onclick: "bought(#{request.id},#{!request.book.bought})" %>
